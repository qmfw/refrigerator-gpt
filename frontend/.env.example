# FridgeGPT Frontend Environment Configuration
# Copy this file to .env and update with your values
# 
# IMPORTANT: Never commit .env to version control!

# ============================================================================
# Backend API Configuration
# ============================================================================

# Option 1: Full Base URL (Recommended - simplest)
# Uncomment and set your full API URL:
# API_BASE_URL=https://api.yourdomain.com/api/v1

# Option 2: Individual Components (if you prefer granular control)
# Uncomment and configure each component:

# API Host (default: localhost)
API_HOST=localhost

# API Port (default: 8000)
API_PORT=8000

# API Protocol (default: http in debug, https in release)
# Options: http, https
API_PROTOCOL=http

# API Path (default: /api/v1)
API_PATH=/api/v1

# ============================================================================
# Development vs Production
# ============================================================================

# For local development:
# API_HOST=localhost
# API_PORT=8000
# API_PROTOCOL=http
# API_PATH=/api/v1

# For production:
# API_BASE_URL=https://api.yourdomain.com/api/v1
# OR
# API_HOST=api.yourdomain.com
# API_PORT=443
# API_PROTOCOL=https
# API_PATH=/api/v1

# ============================================================================
# Notes
# ============================================================================
# - If API_BASE_URL is set, it takes priority over individual components
# - If API_BASE_URL is not set, the URL is constructed from API_HOST + API_PORT + API_PROTOCOL + API_PATH
# - Defaults are used if any variable is missing
# - Protocol defaults to 'http' in debug mode, 'https' in release mode
